<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            line-height: 1.6;
        }

        .container {
            text-align: center;
            padding: 2rem;
            max-width: 600px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        .logo {
            width: 80px;
            height: 80px;
            margin: 0 auto 2rem;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid rgba(255, 255, 255, 0.3);
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .description {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
            font-weight: 300;
        }

        .progress-bar {
            width: 100%;
            height: 4px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 2px;
            margin: 2rem 0;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #00f5ff, #00d4aa);
            border-radius: 2px;
            animation: progress 3s ease-in-out infinite;
        }

        @keyframes progress {
            0% { width: 0%; }
            50% { width: 70%; }
            100% { width: 0%; }
        }

        .status-text {
            font-size: 0.9rem;
            margin-bottom: 2rem;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 500;
        }

        {{#if countdown}}
        .countdown {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin: 2rem 0;
            flex-wrap: wrap;
        }

        .countdown-item {
            background: rgba(255, 255, 255, 0.15);
            padding: 1rem;
            border-radius: 10px;
            min-width: 80px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .countdown-number {
            display: block;
            font-size: 2rem;
            font-weight: bold;
            color: #00f5ff;
        }

        .countdown-label {
            font-size: 0.8rem;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        {{/if}}

        {{#if emailAddress}}
        .contact {
            margin: 2rem 0;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .contact-link {
            color: #00f5ff;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .contact-link:hover {
            color: #00d4aa;
            text-shadow: 0 0 10px rgba(0, 245, 255, 0.3);
        }
        {{/if}}

        {{#if socials}}
        .socials {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin: 2rem 0;
            flex-wrap: wrap;
        }

        .social-link {
            display: inline-block;
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 50%;
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            font-size: 1.2rem;
        }

        .social-link:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        {{/if}}

        .footer {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            font-size: 0.9rem;
            opacity: 0.7;
        }

        @media (max-width: 768px) {
            .container {
                margin: 1rem;
                padding: 1.5rem;
            }

            h1 {
                font-size: 2rem;
            }

            .description {
                font-size: 1rem;
            }

            {{#if countdown}}
            .countdown {
                gap: 0.5rem;
            }

            .countdown-item {
                min-width: 60px;
                padding: 0.8rem;
            }

            .countdown-number {
                font-size: 1.5rem;
            }
            {{/if}}
        }
    </style>
</head>
<body>
    <div class="container">
        {{#if logo}}
        <img src="{{logo}}" alt="Logo" class="logo">
        {{/if}}

        <h1>{{title}}</h1>
        <p class="description">{{description}}</p>

        <div class="progress-bar">
            <div class="progress-fill"></div>
        </div>

        <div class="status-text">System Maintenance in Progress</div>

        {{#if countdown}}
        <div class="countdown">
            <div class="countdown-item">
                <span class="countdown-number" id="days">--</span>
                <span class="countdown-label">Days</span>
            </div>
            <div class="countdown-item">
                <span class="countdown-number" id="hours">--</span>
                <span class="countdown-label">Hours</span>
            </div>
            <div class="countdown-item">
                <span class="countdown-number" id="minutes">--</span>
                <span class="countdown-label">Minutes</span>
            </div>
            <div class="countdown-item">
                <span class="countdown-number" id="seconds">--</span>
                <span class="countdown-label">Seconds</span>
            </div>
        </div>

        <script>
            const countdownDate = new Date("{{countdown}}").getTime();
            
            function updateCountdown() {
                const now = new Date().getTime();
                const distance = countdownDate - now;
                
                if (distance < 0) {
                    location.reload();
                    return;
                }
                
                const days = Math.floor(distance / (1000 * 60 * 60 * 24));
                const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((distance % (1000 * 60)) / 1000);
                
                document.getElementById("days").textContent = days.toString().padStart(2, '0');
                document.getElementById("hours").textContent = hours.toString().padStart(2, '0');
                document.getElementById("minutes").textContent = minutes.toString().padStart(2, '0');
                document.getElementById("seconds").textContent = seconds.toString().padStart(2, '0');
            }
            
            updateCountdown();
            setInterval(updateCountdown, 1000);
        </script>
        {{/if}}

        {{#if emailAddress}}
        <div class="contact">
            <p>{{emailText}}</p>
            <a href="mailto:{{emailAddress}}" class="contact-link">{{emailAddress}}</a>
        </div>
        {{/if}}

        {{#if socials}}
        <div class="socials">
            {{#if socials.facebook}}
            <a href="{{socials.facebook}}" class="social-link" target="_blank" rel="noopener">📘</a>
            {{/if}}
            {{#if socials.x}}
            <a href="{{socials.x}}" class="social-link" target="_blank" rel="noopener">🐦</a>
            {{/if}}
            {{#if socials.instagram}}
            <a href="{{socials.instagram}}" class="social-link" target="_blank" rel="noopener">📷</a>
            {{/if}}
            {{#if socials.linkedin}}
            <a href="{{socials.linkedin}}" class="social-link" target="_blank" rel="noopener">💼</a>
            {{/if}}
            {{#if socials.github}}
            <a href="{{socials.github}}" class="social-link" target="_blank" rel="noopener">🐱</a>
            {{/if}}
            {{#if socials.youtube}}
            <a href="{{socials.youtube}}" class="social-link" target="_blank" rel="noopener">📹</a>
            {{/if}}
        </div>
        {{/if}}

        <div class="footer">
            <p>{{copyright}}</p>
        </div>
    </div>
</body>
</html>